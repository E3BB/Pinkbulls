[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

void fragment() {
	COLOR = texture(TEXTURE, UV);
	COLOR *= vec4(0.3, 0.5, 0.7, 0.5);
}

void vertex() {
  VERTEX += vec2(cos(TIME) * 16.0, sin(TIME) * 16.0);
}
"
